# Pin the workspace to the minimum Rust release declared in Cargo.toml. Fuzzing
# (fuzz/) needs nightly and is invoked explicitly with `cargo +nightly`.
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
